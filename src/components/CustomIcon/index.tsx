import { createIconSetFromIcoMoon } from "@expo/vector-icons";
import config from "../../assets/fonts/inter-icons/config.json";

const Icon = createIconSetFromIcoMoon(config, "interIcons", "interIcons");

export default Icon;
